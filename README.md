A test game written in python

**build instructions:**

1. install PyQT6: `pip install PyQT6`

2. install pyinstaller: `pip install pyinstaller`

3. open terminal in the project folder and type `pyinstaller app.pyw --onefile`

4. copy `characters.json` and `gamestate.json` to the same folder as `app.exe`

